@model IEnumerable<QuanLyGiaoVienCSDLNC.Models.Khoa>
@{
    ViewData["Title"] = "Danh sách khoa";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-body">
    <div class="container-fluid">
        <div class="page-title">
            <div class="row">
                <div class="col-6">
                    <h3>Danh sách khoa</h3>
                </div>
                <div class="col-6">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">
                            <a asp-controller="Home" asp-action="Index">
                                <svg class="stroke-icon">
                                    <use href="../assets/svg/icon-sprite.svg#stroke-home"></use>
                                </svg>
                            </a>
                        </li>
                        <li class="breadcrumb-item">Quản lý khoa</li>
                        <li class="breadcrumb-item active">Danh sách</li>
                    </ol>
                </div>
            </div>
        </div>
    </div>
    <!-- Container-fluid starts-->
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-12">
                @if (TempData["SuccessMessage"] != null)
                {
                    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
                }
                @if (TempData["ErrorMessage"] != null)
                {
                    <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
                }
                <div class="card">
                    <div class="card-header">
                        <div class="d-flex justify-content-between">
                            <h5>Danh sách khoa</h5>
                            <a asp-action="Create" class="btn btn-primary">Thêm mới</a>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>Mã khoa</th>
                                        <th>Tên khoa</th>
                                        <th>Địa chỉ</th>
                                        <th>Chủ nhiệm khoa</th>
                                        <th>Chức năng</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@item.MaKhoa</td>
                                            <td>@item.TenKhoa</td>
                                            <td>@item.DiaChi</td>
                                            <td>@item.MaChuNhiemKhoa</td>
                                            <td>
                                                <a asp-action="Edit" asp-route-id="@item.MaKhoa" class="btn btn-info btn-sm">Sửa</a>
                                                <a asp-action="Details" asp-route-id="@item.MaKhoa" class="btn btn-primary btn-sm">Chi tiết</a>
                                                <a asp-action="Delete" asp-route-id="@item.MaKhoa" class="btn btn-danger btn-sm">Xóa</a>
                                                <a asp-action="PhanCongChuNhiem" asp-route-id="@item.MaKhoa" class="btn btn-warning btn-sm">Phân công</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Container-fluid Ends-->
</div>